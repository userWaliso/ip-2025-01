package main
import (
	f"fmt"
)
func main() {
	var (
		x int
		vetor[] int
		comp[] int
	)
	for i:=0;i<10;i++ {
		f.Scan(&x)
		vetor = append(vetor, x)
	}

	for k,v := range vetor {

	}
	// f.Print(vetor)
}