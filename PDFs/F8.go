package main 
import (
	f "fmt"
	m "math"
)

func norm
func main() {
	var n, m int
	f.Scan(&n,&m)
	matriz := make([][]float64, n)

	for i := 0; i < n; i++ {
		matriz[i] = make([]float64, m)
		for j := 0; j < m; j++ {
			f.Scan(&matriz[i][j])
		}
	}
	for i := 0; i < n; i++ {
		for j := 0; j < m; j++ {

		}
	}


}